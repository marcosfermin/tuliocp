# Fail2Ban configuration file for tulio

[Definition]

actionstart = /usr/local/tulio/bin/v-add-firewall-chain <name>
actionstop = /usr/local/tulio/bin/v-delete-firewall-chain <name>
actioncheck = iptables -n -L INPUT | grep -q 'fail2ban-<name>[ \t]'
actionban = /usr/local/tulio/bin/v-add-firewall-ban <ip> <name>
actionunban = /usr/local/tulio/bin/v-delete-firewall-ban <ip> <name>
