<?php

// Fixture: a configuration file that does not parse. Rewriting a setting in it
// must be refused rather than installed.

$config["password_driver"] = "tulio"
$config["password_tulio_host"] = "localhost";
